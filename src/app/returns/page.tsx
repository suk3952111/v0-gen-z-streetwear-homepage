import { InfoPage } from "@/components/common/info-page"

export default function ReturnsPage() {
  return (
    <InfoPage
      title="Returns"
      description="Returns are accepted within the return window for eligible items in original condition."
    />
  )
}

